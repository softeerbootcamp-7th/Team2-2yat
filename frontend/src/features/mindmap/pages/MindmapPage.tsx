import { MindMapProvider } from "@/features/mindmap/providers/NodeContainerProvider";

const MindmapPage = () => {
    return <MindMapProvider>mindmap 페이지</MindMapProvider>;
};

export default MindmapPage;
